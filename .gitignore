# Memory symlink (each user creates their own)
memory

# Local MCP config (merged from mcp.shared.json + personal)
.mcp.json

# OS files
.DS_Store
*.swp
*~

# Personal secrets
.env
.env.local

# User state
.egregore-state.json
