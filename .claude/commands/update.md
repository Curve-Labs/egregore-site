Update local Egregore environment. Follow the /update command instructions in skills/cl-collaborator/SKILL.md.

This command:
1. Runs /pull (smart sync all repos)
2. Merges mcp.shared.json into local .mcp.json (keeps personal MCPs)
3. Reports what changed
4. Reminds to restart if MCPs changed
